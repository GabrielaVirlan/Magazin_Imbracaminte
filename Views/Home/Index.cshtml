@model IEnumerable<MagazinImbracaminte.Models.Product>

@{
    ViewData["Title"] = "Home";
}

<figure class="text-center">
    <figcaption>
        @Html.DisplayFor(modelItem => Model.ElementAt(0).Name)
    </figcaption>
    <br />
    <img src="@Model.ElementAt(0).Image" asp-append-version="true" />
</figure>


<figure class="text-center">
    <figcaption>
        @Html.DisplayFor(modelItem => Model.ElementAt(1).Name)
    </figcaption>
    <br />
    <img src="@Model.ElementAt(1).Image" asp-append-version="true" />
</figure>

<figure class="text-center">
    <figcaption>
        @Html.DisplayFor(modelItem => Model.ElementAt(2).Name)
    </figcaption>
    <br />
    <img src="@Model.ElementAt(2).Image" asp-append-version="true" />
</figure>